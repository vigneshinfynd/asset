extends ../layout

block content
  .card
    .card-body
      h4.text-danger Are you sure you want to delete this asset?
      p This action cannot be undone.
      form(method="POST", action=`/assets/${asset.id}/delete`)
        button.btn.btn-danger(type="submit") Confirm Delete
        a.btn.btn-secondary(href="/assets") Cancel
